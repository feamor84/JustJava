<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="activity_main">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="text_views">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/default_text_size</item>
    </style>

    <style name="text_views.heading" parent="text_views">
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/colorPrimaryLight</item>
    </style>

    <style name="text_views.heading.quantity" parent="text_views.heading">
        <item name="android:text">@string/heading_quantity</item>
    </style>

    <style name="text_views.heading.price" parent="text_views.heading">
        <item name="android:text">@string/heading_price</item>
    </style>

    <style name="text_views.quantity" parent="text_views">
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:text">@string/quantity</item>
    </style>

    <style name="quantity_wrapper">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/default_text_size</item>
    </style>

    <style name="button.order" parent="button">
        <item name="android:text">@string/order</item>
    </style>

    <style name="button.small">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:textSize">@dimen/default_text_size</item>
    </style>

    <style name="button.small.decrement" parent="button.small">
        <item name="android:text">-</item>
        <item name="android:layout_marginRight">16dp</item>
    </style>

    <style name="button.small.increment" parent="button.small">
        <item name="android:text">+</item>
        <item name="android:layout_marginLeft">16dp</item>
    </style>

</resources>
